<%= form_for(@hof) do |f| %>
  <% if @hof.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hof.errors.count, "error") %> prohibited this hof from being saved:</h2>

      <ul>
      <% @hof.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
